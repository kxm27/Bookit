void main() {
  runApp(MaterialApp(
    debugShowCheckedModeBanner: false,
    title: 'Bookit',
    home: BookitPage(),
  ));
}

class BookitPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Image.asset('assets/app_logo.png', fit: BoxFit.cover),
      ),
      body: Center(
        child: Column(
          mainAxisAlignment: MainAxisAlignment.center,
          children: <Widget>[
            Image.asset(
              'Assets/welcome.png',
              width: 300,
              height: 300,
            ), // Add your Bookit logo here
            SizedBox(
              width: 200.0,
              height: 50.0,
              child: ElevatedButton(
                child: Text('login'),
                onPressed: () {},
              ),
            ),
            SizedBox(height: 15),
            SizedBox(
              width: 200.0,
              height: 50.0,
              child: ElevatedButton(
                child: Text('sign up'),
                onPressed: () {},
              ),
            ),
          ],
        ),
      ),
    );
  }
}
